import 'package:flutter/material.dart';

class AppShadows {
  static const shadow48 = BoxShadow(
    color: Color(0x14000000),
    offset: Offset(0, 8),
    blurRadius: 48,
    spreadRadius: 0,
  );

  static const shadow16 = BoxShadow(
    color: Color(0x29000000),
    offset: Offset(0, 4),
    blurRadius: 16,
    spreadRadius: 0,
  );

  static const shadow24 = BoxShadow(
    color: Color(0x1f000000),
    offset: Offset(0, 4),
    blurRadius: 24,
    spreadRadius: 0,
  );

  static const shadow32 = BoxShadow(
    color: Color(0x1f000000),
    offset: Offset(0, 8),
    blurRadius: 32,
    spreadRadius: 0,
  );

  AppShadows._();
}
